<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Internal cache directory for temporary camera photos -->
    <cache-path name="camera_images" path="camera/" />

    <!-- Internal files directory for saved recipe photos -->
    <files-path name="recipe_images" path="images/" />

    <!-- External cache for large media files (if needed) -->
    <external-cache-path name="external_cache" path="/" />
</paths>
